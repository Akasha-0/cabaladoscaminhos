{
  "name": "cabaladoscaminhos-w71-media",
  "version": "71.0.0",
  "description": "W71-C: Audio + video posts (record, upload, playback, waveform) - cabaladoscaminhos engines slice",
  "private": true,
  "type": "module",
  "scripts": {
    "build:check": "tsc --noEmit --skipLibCheck",
    "smoke": "node --experimental-strip-types ./smoke/media-smoke.ts"
  },
  "engines": {
    "node": ">=22"
  }
}
