{
  "name": "cabaladoscaminhos-w71-live",
  "version": "71.0.0",
  "description": "W71-D: Live streaming (WebRTC peer-to-peer + RTMP ingest stub + chat integration + VOD recording) - cabaladoscaminhos engines slice",
  "private": true,
  "type": "module",
  "scripts": {
    "build:check": "tsc --noEmit --skipLibCheck",
    "smoke": "node --experimental-strip-types ./smoke/live-smoke.ts"
  },
  "engines": {
    "node": ">=22"
  }
}