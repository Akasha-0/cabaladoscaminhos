{
  "name": "@cabaladoscaminhos/w71-push",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "W71-B: Real Web Push (Service Worker + VAPID + ECE + permission flow) for Cabala dos Caminhos",
  "scripts": {
    "smoke": "node --experimental-strip-types ./smoke/push-smoke.ts",
    "tsc": "tsc --noEmit --skipLibCheck"
  }
}