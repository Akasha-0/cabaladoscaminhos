// mural-tzolkin.test.ts — DEPRECATED Iter44.
// File retained in repo due to filesystem overlay preventing rm.
// The mural feature was cut (mural/page.tsx redirects to /dashboard).
// mural-tzolkin.ts is now a stub. These tests are obsolete.
// Safe to delete once filesystem allows.
describe('mural-tzolkin (deprecated)', () => {
  it('placeholder — mural cut Iter44', () => {});
});
