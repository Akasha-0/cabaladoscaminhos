You are extracting ONE helper module from ONE oversized file in the AKASHA project.
Target: apps/akasha-portal/src/lib/application/life-areas/life-areas-engine/life-areas-data.ts (809 lines)

RULES — EXACTLY FOLLOW:
1. Run `git status` before starting — if file was modified, skip this task entirely
2. Read the target file and identify ONE natural group of related functions (50-150 lines)
3. Create a new helper file in the same directory: e.g. synthesis-engine/<name>.ts
4. Update the original file to import from the new helper
5. Run `pnpm typecheck` to verify — must pass
6. Run `pnpm test:run` — must not break existing tests
7. Commit: `git add <newfile> <targetfile> && git commit -m 'refactor: extract helper from <target_file>'`
8. Write to /home/skynet/cabala-dos-caminhos/.autonomous/multi-agent/agent-results/result-ta-1781658387-0.json:
{"success": true, "message": "Extracted helper from apps/akasha-portal/src/lib/application/life-areas/life-areas-engine/life-areas-data.ts", "type": "large_file"}
9. If timeout near (30s left), write task-result.json FIRST then finish

DO NOT split more than ONE file. DO NOT extract more than ONE helper.
CRITICAL: Write task-result.json BEFORE the process exits.
