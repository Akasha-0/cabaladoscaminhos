You are adding test coverage in the AKASHA project at /home/skynet/cabala-dos-caminhos.
Target files: packages/core-iching/src/practices-cromoterapia.ts, packages/core-iching/src/practices-data.ts

RULES:
1. Run `git status` before starting — if file was modified by another agent, skip it
2. Use `codegraph_explore` to understand what each file exports and its dependencies
3. Create vitest test file at: <file>.test.ts (same dir, same name + .test.ts)
4. Each test file must:
   - Import the module under test
   - Test 2-3 main exported functions with real assertions
   - Cover ONE edge case each (empty input, error case, boundary condition)
5. Run `pnpm test:run` to verify tests pass
6. Commit: `git add <testfile> && git commit -m 'test: add coverage for <filename>'`
7. Write to /home/skynet/cabala-dos-caminhos/.autonomous/multi-agent/agent-results/result-ta-1781645265-0.json:
{"success": true, "message": "Added tests for N files", "type": "missing_tests"}
8. If timeout near (30s left), write task-result.json FIRST then finish

CRITICAL: Write task-result.json BEFORE the process exits.
