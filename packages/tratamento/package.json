{
  "name": "@akasha/tratamento",
  "version": "0.1.0",
  "description": "Akasha — Synthesis Engine (tratamento): orquestra o corpus terapêutico em 7 camadas de saída PT-BR",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:run": "vitest run --project unit --no-coverage",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@akasha/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
