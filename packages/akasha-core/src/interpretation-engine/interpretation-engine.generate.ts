/**
 * @akasha/core — Interpretation Engine: generate
 *
 * Funções de geração de interpretação (query functions).
 * Re-exporta de ./queries.ts para manter o facade limpo.
 */
export { interpretarVida, interpretarVidaArea } from './queries';
