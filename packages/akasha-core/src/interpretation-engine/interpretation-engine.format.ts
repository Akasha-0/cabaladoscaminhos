/**
 * @akasha/core — Interpretation Engine: format
 *
 * Helpers de formatação que montam AreaInterpretation a partir de
 * NumeroContent ou de fallbacks genéricos.
 * Re-exporta de ./builders.ts para manter o facade limpo.
 */
export { buildInterpretation, buildFallback, baseInterpretation } from './builders';
