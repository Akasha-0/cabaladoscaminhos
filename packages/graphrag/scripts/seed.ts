#!/usr/bin/env tsx
/**
 * @akasha/graphrag — seed script (executable via `pnpm seed`).
 *
 * Wrapper sobre src/seed/run-seed.ts — mantido em scripts/ por
 * convenção pnpm (executáveis ficam aqui, código em src/).
 */

import "../src/seed/run-seed";