{
  "name": "@akasha/graphrag",
  "version": "0.0.1",
  "description": "Akasha — GraphRAG MVP: hybrid retrieval (graph + vector) sobre pgvector (Postgres). Wave 31.1.",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "seed": "tsx scripts/seed.ts"
  },
  "dependencies": {
    "@akasha/types": "workspace:*",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.22.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "peerDependencies": {
    "@akasha/types": "*"
  }
}
