/**
 * MiniMax M3 API integration.
 * STUB: Implementação real na Onda 3 (Oráculo Vivo).
 */

import type { ChatMessage } from './types';

export async function generateMinimaxResponse(
  messages: ChatMessage[],
  _options?: { temperature?: number; maxTokens?: number }
): Promise<string> {
  // Stub: retorna resposta vazia para evitar crashes em dev
  void messages;
  return '[MiniMax stub — implementação pendente]';
}
