{
  "buildCommand": "npm run build --prefix apps/akasha-portal",
  "devCommand": "npm run dev --prefix apps/akasha-portal",
  "installCommand": "npm install --legacy-peer-deps",
  "crons": [
    {
      "path": "/api/cron/cleanup-tokens",
      "schedule": "0 0 * * *"
    }
  ]
}