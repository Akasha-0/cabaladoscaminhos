'use client';

/**
 * ============================================================================
 * Badge (Design System wrapper)
 * ============================================================================
 * Re-export so consumers can `import { Badge } from '@/components/design-system'`.
 * ============================================================================
 */

export { Badge, badgeVariants } from '@/components/ui/badge';
