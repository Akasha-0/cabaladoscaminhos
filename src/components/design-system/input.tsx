'use client';

/**
 * ============================================================================
 * Input (Design System wrapper)
 * ============================================================================
 * Re-export so consumers can `import { Input } from '@/components/design-system'`.
 * ============================================================================
 */

export { Input } from '@/components/ui/input';
